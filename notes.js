// what is state?.
// 1. statefull protocol 
// state ful protocol require server to save the status and session information
// examaple : ftp- file transfor protocol 

// 2. stateless Protocol
// Stateless protocol doesn't not require the server to retain the server information or 

// example : http : HyperText Transfer Protocol.


//Let’s implement authentication and authorization using popular npm packages in your Express + MongoDB + EJS app (like Wanderlust).

// ---------> npm install passport passport-local passport-local-mongoose express-session
